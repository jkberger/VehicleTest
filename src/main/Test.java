package main;

import static org.junit.Assert.*;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

public class Test {

	a = new Car(4,Person a );
	hashmap.add(a);
	
	return hashmap.length
	}

}
